<?php
$page['dir']='\\habblet';require_once('../includes/core.php');$db=new Database();if(!$user->logged_in){http_response_code(401);exit('Login required');}$reported=(int)($_POST['reported_user_id']??0);$reason=trim((string)($_POST['reason']??''));$evidence=trim((string)($_POST['evidence']??''));if($reported<1||$reported===(int)$user->id||$reason===''||$evidence===''){http_response_code(422);exit('Invalid report');}if($db->fetchColumn('SELECT id FROM users WHERE id=?',[$reported])===false){http_response_code(404);exit('User not found');}$db->execute('INSERT INTO phpretro_user_reports (reporter_id,reported_user_id,reason,evidence,status,action_notes,created_at) VALUES (?,?,?,?,?,?,?)',[(int)$user->id,$reported,$reason,$evidence,'open','',time()]);header('Content-Type: application/json');echo json_encode(['ok'=>true]);